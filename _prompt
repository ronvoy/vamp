make a README.md file that explains the workflow process flow also in mermaid diagram and tabular for of each component description of a voice to app maker using LLM and AI coding agent and how to implement each steps and caveats, then at the end how to run it. After than make a single shell script that would make a python flask based server that would constantly fetch the user voice like in a normal conversation which is translated and transcribed by llm and send to a selected llm agent that is also said under the voice command excerpt in the voice command query and then the designated llm shall be called to start start coding and give the output result that will be saved in conversation folder list (appropriate name also generated by the same AI coding agent) with it's code ready to be tested and deployed.

---------------------

forgot to mention, sorry. We implement those call to llm using openrouter with openrouter api key in .env file